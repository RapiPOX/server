// Types
import { ActionIndex } from '../types/actions';

// TODO: Get all actions from the actions folder
import http from './http';

const actions: ActionIndex = {
  http,
};

export default actions;
